@* @using Application.Services.ClassServices
@using Application.DTOs
@inject IClassService ClassService
@page "/classs/{id:int}"


<a href="/classes" class="btn btn-secondary">Back to List</a>

<div class="container mt-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white">
            <h3 class="mb-0">Class Details</h3>
        </div>
        <div class="card-body">
            @if (class != null)
            {
                <div class="row mb-3">
                    <div class="col-md-4 font-weight-bold">Id:</div>
                    <div class="col-md-8">@classe</div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-4 font-weight-bold">Name:</div>
                    <div class="col-md-8">@class.Name</div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-4 font-weight-bold">Sex:</div>
                    <div class="col-md-8">@class.Sex</div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-4 font-weight-bold">Date Of Birth:</div>
                    <div class="col-md-8">@class.DateOfBirth</div>
                </div>
                
                
                <div class="row">
                    <div class="col text-right">
                        <a href="/classs/update/@class.Id"><button class="btn btn-primary">Edit</button></a>
                        <a href="/classs/delete/@class.Id"><button class="btn btn-danger">Delete</button></a>
                        <a href="#"><button class="btn btn-secondary ">Restore</button></a>
                    </div>
                </div>
            }
            else
            {
                <p>Loading Class details...</p>
            }
        </div>
    </div>
</div>


<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">



@code {
    [Parameter]
    public int Id { get; set; }
    private Classes? classe;
    protected override void OnParametersSet()
    {
        classe =  ClassService.GetclassById(Id);
    }

    
} *@